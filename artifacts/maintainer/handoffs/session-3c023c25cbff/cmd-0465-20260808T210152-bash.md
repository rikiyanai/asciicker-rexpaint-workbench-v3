# Command Handoff

- Session key: `3c023c25cbff`
- Sequence: `0465`
- Timestamp (UTC): `2026-08-08T21:01:52.600710+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "python3 -c \"\nimport sys\nsys.path.insert(0,'scripts')\nfrom xp_core import XPFile\nMAG=(255,0,255); EMPTY=(0,32); KEY=(255,255,85)\ndef audit(name,p,cw,ch):\n    xp=XPFile(); xp.load(p); v=xp.layers[2]; key=tuple(xp.layers[0].data[0][0][2])\n    half=9; tot=cw*ch\n    def cell(r,c,dy,dx): return v.data[r*ch+dy][c*cw+dx]\n    def mask(r,c): return [[(cell(r,c,dy,dx)[0] not in EMPTY) or (tuple(cell(r,c,dy,dx)[2]) not in (MAG,key)) for dx in range(cw)] for dy in range(ch)]\n    def d(a,b): return sum(1 for ra,rb in zip(a,b) for x,y in zip(ra,rb) if x!=y)\n    geo=n=cbad=cn=0\n    for r in range(8):\n        for c in range(half):\n            geo+=d(mask(r,c)[::-1],mask(r,c+half)); n+=tot\n            for dy in range(ch):\n                for dx in range(cw):\n                    g0,f0,b0=cell(r,c,dy,dx); g1,f1,b1=cell(r,c+half,ch-1-dy,dx)\n                    if g0 in EMPTY and tuple(b0) in (MAG,key): continue\n                    cn+=1\n                    if tuple(f0)!=tuple(f1) or tuple(b0)!=tuple(b1): cbad+=1\n    yellow=sum(1 for y in range(v.height) for x in range(v.width) if tuple(v.data[y][x][2])==KEY)\n    print(f'{name:16s} geo {100*geo/n:4.1f}%  colour {100*cbad/cn:4.1f}%  yellow-key cells {yellow}')\naudit('gromit','data/exports/session-5050301b-a087-4c69-8976-15256c0641db.xp',10,12)\naudit('wallace','data/exports/session-7a7dd262-6c1c-47f4-b2f5-3a267c01c373.xp',7,9)\n\" 2>&1 | rg -v '^Loading|^Loaded' && cp data/exports/session-5050301b-a087-4c69-8976-15256c0641db.xp adhoc/2026-06-08-gromit-repaired.xp && cp data/exports/session-7a7dd262-6c1c-47f4-b2f5-3a267c01c373.xp adhoc/2026-06-08-wallace-repaired.xp && echo staged",
  "preview": "python3 -c \" import sys sys.path.insert(0,'scripts') from xp_core import XPFile MAG=(255,0,255); EMPTY=(0,32); KEY=(255,255,85) def audit(name,p,cw,ch): xp=XPFile(); xp.load(p); v=xp.layers[2]; key=tuple(xp.layers[0].data[0][0][2]) half=9; tot=cw*ch def cell(r,c,dy,dx): return v.data[r*ch+dy][c*c..."
}
```
