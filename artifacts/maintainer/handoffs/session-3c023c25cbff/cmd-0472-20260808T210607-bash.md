# Command Handoff

- Session key: `3c023c25cbff`
- Sequence: `0472`
- Timestamp (UTC): `2026-08-08T21:06:07.084282+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "git branch --show-current && git rev-parse --short HEAD && echo \"--- status (tracked changes) ---\" && git status --porcelain | rg -v '^\\?\\?' | head -20 && echo \"--- untracked of interest ---\" && git status --porcelain | rg '^\\?\\?' | rg -i 'adhoc|exports|sessions' | head -10 && echo \"--- ignored? ---\" && git check-ignore -v adhoc/2026-06-08-gromit-repaired.xp data/exports/session-5050301b-a087-4c69-8976-15256c0641db.xp 2>&1 | head",
  "preview": "git branch --show-current && git rev-parse --short HEAD && echo \"--- status (tracked changes) ---\" && git status --porcelain | rg -v '^\\?\\?' | head -20 && echo \"--- untracked of interest ---\" && git status --porcelain | rg '^\\?\\?' | rg -i 'adhoc|exports|sessions' | head -10 && echo \"--- ignored? ..."
}
```
