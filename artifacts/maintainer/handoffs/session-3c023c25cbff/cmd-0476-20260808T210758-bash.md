# Command Handoff

- Session key: `3c023c25cbff`
- Sequence: `0476`
- Timestamp (UTC): `2026-08-08T21:07:58.876723+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "python3 - <<'PY'\nfrom pathlib import Path\n# Take the source sheets from argv/env instead of hardcoding an out-of-repo\n# absolute path. docs/INDEX.md requires this repo to be self-contained, and the\n# self-containment audit blocks commits that embed external paths.\nfor name, var, default_note in ((\"adhoc/repair.py\", \"GROMIT_SRC\", \"gromit\"),\n                                (\"adhoc/wallace2.py\", \"WALLACE_SRC\", \"wallace\")):\n    p = Path(name); s = p.read_text()\n    if \"os.environ\" in s:\n        continue\n    s = s.replace(\"import shutil\\nimport sys\",\n                  \"import os\\nimport shutil\\nimport sys\")\n    s = s.replace('\"/Users/r/Desktop/2026-06-08-gromit.xp\"',\n                  'os.environ.get(\"GROMIT_SRC\", \"sprites/wolfie.xp\")')\n    s = s.replace('\"/Users/r/Desktop/2026-06-08-wallace.xp\"',\n                  'os.environ.get(\"WALLACE_SRC\", \"sprites/player-0000.xp\")')\n    p.write_text(s)\n    print(\"parameterised\", name)\nPY\nrg -n 'Users/r/Desktop' adhoc/*.py | head; echo \"--- my files clean? ---\"; python3 scripts/self_containment_audit.py 2>&1 | rg 'adhoc/' | head",
  "preview": "python3 - <<'PY' from pathlib import Path # Take the source sheets from argv/env instead of hardcoding an out-of-repo # absolute path. docs/INDEX.md requires this repo to be self-contained, and the # self-containment audit blocks commits that embed external paths. for name, var, default_note in (..."
}
```
