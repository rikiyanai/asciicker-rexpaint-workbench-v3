# Command Handoff

- Session key: `3c023c25cbff`
- Sequence: `0478`
- Timestamp (UTC): `2026-08-08T21:08:48.683845+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "ls -la .git/hooks/pre-commit 2>/dev/null && head -5 .git/hooks/pre-commit 2>/dev/null; echo \"--- my adhoc files clean of external paths? ---\"; python3 scripts/self_containment_audit.py 2>&1 | rg -c 'adhoc/' || echo \"0 adhoc violations\"; echo \"--- blocking errors NOT mine, by area ---\"; python3 scripts/self_containment_audit.py 2>&1 | rg '^- \\[' | rg -v 'adhoc/' | sed 's/:.*//' | sed 's/.*\\] //' | sed 's#/.*##' | sort | uniq -c | sort -rn | head",
  "preview": "ls -la .git/hooks/pre-commit 2>/dev/null && head -5 .git/hooks/pre-commit 2>/dev/null; echo \"--- my adhoc files clean of external paths? ---\"; python3 scripts/self_containment_audit.py 2>&1 | rg -c 'adhoc/' || echo \"0 adhoc violations\"; echo \"--- blocking errors NOT mine, by area ---\"; python3 sc..."
}
```
