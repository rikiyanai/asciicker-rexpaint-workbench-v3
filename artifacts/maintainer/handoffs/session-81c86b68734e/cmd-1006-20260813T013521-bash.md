# Command Handoff

- Session key: `81c86b68734e`
- Sequence: `1006`
- Timestamp (UTC): `2026-08-13T01:35:21.886619+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "cat >> .scratch/2026-08-13-headed-proof.md <<'MDEOF'\n\n---\n\n## 13. Wallace from-scratch mobile recipe \u2014 live execution\n\nPhase-2 execution of `.scratch/2026-08-13-wallace-recipe/recipe.json` (3031 actions) end-to-end\nthrough the real headed **mobile** WebKit workbench, followed by an export through the mobile\nShare control and a byte-level equality check against `sprites/2026-06-08-wallace.xp`.\n\nDriver: `.scratch/2026-08-13-wallace-recipe/execute_full.mjs`.\nLog: `.scratch/2026-08-13-wallace-recipe/execution.log`.\nMachine summary: `.scratch/2026-08-13-wallace-recipe/execution-summary.json`.\n\n### 13.1 Context configuration\n\nPlaywright 1.58.2, `webkit.launch({ headless: false })` \u2014 headed, MOBILE touch context only.\nNo desktop context was created at any point in this run.\n\n```\ncontext     : devices['iPhone 13 landscape'], acceptDownloads: true\nuserAgent   : Mozilla/5.0 (iPhone; CPU iPhone OS 15_0 like Mac OS X) AppleWebKit/605.1.15\n              (KHTML, like Gecko) Version/26.0 Mobile/15E148 Safari/604.1\nviewport    : 750 x 342 CSS px, devicePixelRatio 3\nontouchstart in window                                 : true\nmatchMedia('(pointer: coarse) and (orientation: landscape)') : true   <- pinned-sidebar mobile shell\nserver      : PYTHONPATH=src python3 -m pipeline_v2.app, 127.0.0.1:5071 (self-started, pid 1393)\n```\n\nNote `navigator.maxTouchPoints` reports 0 in this WebKit build; `ontouchstart` and the coarse-pointer\nmedia query are what the workbench's own mobile branches key off, and both are true.\n\n### 13.2 What ran, and where\n\n**3031 / 3031 recipe actions executed in 170.4 s. Zero errors, zero unhandled actions, zero\n`pageerror`s.**\n\n| Recipe range | Shell | Actions | Content |\n|---|---|---|---|\n| 1\u20139 | **ws-advanced** | 9 | `#fsAdvancedBtn`, `#classicGeomAngles/Frames/SourceProjs/CellW/CellH`, `#btnNewXp`, select layer 3, `.ws-layer-del-btn` |\n| 10\u20133031 | **editor-first mobile shell** | 3022 | all of L0, L1, L2 authoring, `#wsSaveBtn`, mobile Share |\n\n`ws-advanced` was entered by the real mobile first-screen button `#fsAdvancedBtn` (recipe step 1) \u2014\nthat is the only route to the classic-geometry row and `#btnNewXp` (obstacle B1 in `audit.md`).\nImmediately after step 9 the run left it again through the real top-bar control\n`.ws-mobile-top-bar [data-action=\"toggle-advanced\"]` \u2014 **one navigation tap that is not part of the\nrecipe**, logged as such; it mutates no document state. The probe taken right after confirms the\neditor-first shell was fully usable for authoring:\n\n```\nSHELL SWITCH advanced true -> false ::\n  {\"adv\":false,\"canvas\":\"visible\",\"glyph\":\"visible\",\"fg\":\"visible\",\"bg\":\"visible\",\n   \"fillSel\":\"visible\",\"rows\":3}\n```\n\nSo **9 of 3031 actions ran in ws-advanced; 3022 ran in the editor-first mobile shell.**\n\n### 13.3 Input fidelity\n\n| Mechanism | Count | What it covered |\n|---|---|---|\n| `page.tap()` \u2014 real Playwright touch tap | 12 | `#fsAdvancedBtn`, `#btnNewXp`, `.ws-layer-del-btn`, `#wsToolSelect` \u00d72, `#wsToolCell` \u00d72, `#wsToolLine`, `#wsFillSel` \u00d72, `#wsSaveBtn`, `[data-action=\"share-file\"]` |\n| `page.tap()` fell back to `element.click()` | 1 | one of the above was not tap-actionable at that moment |\n| `page.fill()` + `change` / `page.selectOption()` \u2014 real typed input | 62 | 4 \u00d7 `#classicGeom*` numeric/text, 1 \u00d7 `#classicGeomSourceProjs` select, 57 \u00d7 `#wsGlyphCode` |\n| value-seed + `input`/`change` dispatch | 51 | `#wsFgColor` / `#wsBgColor` \u2014 `input[type=color]` opens an OS colour picker no automation can drive |\n| `element.click()` on the real `.ws-layer-row` | 4 | `select_layer` (the row carries its own click handler \u2192 `_switchActiveLayer`) |\n| synthetic `PointerEvent` gesture on `#wholeSheetCanvas` | 2827 taps + 74 drags | every painted cell and every `Line` / `Select` drag |\n\nEvery canvas gesture is a full `pointerdown` \u2192 (`pointermove` \u00d72 for drags) \u2192 `pointerup` sequence at\nthe cell's real client coordinates, 16 ms down-time per tap (well under the 500 ms tap-hold revert\nthreshold documented in `audit.md`).\n\n### 13.4 Checkpoints\n\n14 checkpoints. Each one pulled `__wholeSheetEditor.getDocumentSnapshot()` and compared **all three\nlayers, all 9072 cells each**, against an independent in-driver simulation of the recipe. Any\nnon-zero total would have stopped the run and captured a `wm-FAIL-*` screenshot.\n\n```\nCHECKPOINT baseline-blank @step    9: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L1-complete    @step  114: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step  388: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step  645: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step  984: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 1290: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 1564: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 1784: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 2043: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 2291: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 2533: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 2754: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-progress    @step 2964: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\nCHECKPOINT L2-complete    @step 3029: 126x72 layers=3 mismatches=0 [L0:0 L1:0 L2:0]\n```\n\nRecipe step 11 (`assert_toggles`) also passed live:\n`[\"ws-toggle ws-toggle-on\",\"ws-toggle ws-toggle-on\",\"ws-toggle ws-toggle-on\"]` \u2014 G/F/B all on.\n\n### 13.5 Export capture\n\nRecipe step 3030 tapped `#wsSaveBtn`; step 3031 tapped `.ws-mobile-top-bar [data-action=\"share-file\"]`\n(`shareXpFileLocal()`), which forced a `save-session` with `wait_for_idle` and then ran the server\nexport path. Observed on the wire:\n\n```\n[response] export-xp   200\n[response] download-xp 200  2452 bytes\n```\n\nNo `download` event fired, because this WebKit build satisfies the mobile branch of\n`persistence.mjs:shareXpFile` \u2014 probed directly:\n`{\"hasShare\":true,\"hasCanShare\":true,\"canShareFiles\":true}` \u2014 so `navigator.share({files})` consumed\nthe file rather than falling back to `_downloadBlob`. The captured 2452 bytes are the exact\n`GET /api/workbench/download-xp` response body that `_getExportedXpBytes()` fetched and handed to\n`shareXpFile()` \u2014 i.e. the same buffer the share sheet received.\nSaved as `.scratch/2026-08-13-wallace-recipe/exported-wallace.xp`.\n\n### 13.6 EQUALITY \u2014 the headline\n\n`.scratch/2026-08-13-wallace-recipe/equality.py` \u2192 `equality.json`:\n\n```\ntarget   gzip 2426 bytes  sha256 0e2bd782\u2026adbc3a\nexported gzip 2452 bytes  sha256 527bdb80\u20263722f05      gzip_identical: False   (expected)\n\ntarget   payload 272192 bytes  sha256 babb3dc8c73ef09cd12cef836e854bb8d2b0e416fdcb7fb3b56c18dc273c045b\nexported payload 272192 bytes  sha256 babb3dc8c73ef09cd12cef836e854bb8d2b0e416fdcb7fb3b56c18dc273c045b\nPAYLOAD_BYTE_IDENTICAL: True\n\ndims  target [126,72,3] == exported [126,72,3]\nL0: 0 / 9072 mismatches\nL1: 0 / 9072 mismatches\nL2: 0 / 9072 mismatches\ntotal_cell_mismatches: 0 / 27216\nVERDICT: PASS\n```\n\n**`gzip.decompress(exported) == gzip.decompress(target)` \u2014 the uncompressed XP payload is\nbyte-identical.** This is exactly the equality contract stated in `audit.md` \u00a73; the gzip wrapper\ndiffers only by mtime and the embedded FNAME, neither of which is controllable from the UI\n(`xp_codec.py:73` uses `gzip.open(path,'wb')`).\n\n### 13.7 Artifacts\n\nAll under `.scratch/2026-08-13-wallace-recipe/`:\n\n| File | What it shows |\n|---|---|\n| `wm-01-first-screen.png` | mobile first screen (\"XPEdit\"), before any action |\n| `wm-02-baseline-blank.png` | the from-scratch 126\u00d772 / 3-layer blank document (after step 9) |\n| `wm-03-editor-first-shell.png` | immediately after leaving ws-advanced \u2014 editor-first shell, blank doc |\n| `wm-04-after-L0.png` | Layer 0 complete (see caveat 13.9 c) |\n| `wm-05-after-L1.png` | Layers 0+1 complete (after step 114) |\n| `wm-06-tools-drawer-glyph-color.png` | the pinned tools sidebar with `#wsGlyphCode` / `#wsFgColor` / `#wsBgColor` in use |\n| `wm-07-mid-L2-a.png` | mid-Layer-2 checkpoint (~step 1290) |\n| `wm-08-mid-L2-b.png` | mid-Layer-2 checkpoint (~step 2291) |\n| `wm-09-final-fit.png` | full sheet after step 3029, zoomed to Fit via the real `#wsCanvasZoomInput` range control |\n| `wm-10-after-export.png` | state after the mobile Share export |\n| `exported-wallace.xp` | the 2452 bytes the mobile Share control produced |\n| `wallace-target-vs-authored.png` | contact image: target on top, mobile-authored export below |\n| `execution.log`, `execution-summary.json`, `equality.json` | full run record |\n| `execute_full.mjs`, `equality.py`, `render.py`, `execute_l0_shot.mjs`, `shareprobe.mjs` | harnesses |\n\n`wm-09-final-fit.png` is the human-visible \"authored in the mobile workbench\" evidence.\n\n### 13.8 Tally \u2014 27 PASS / 0 FAIL / 6 INFO\n\nPASS (expectation \u2192 observed):\n\n1. Mobile touch context, not desktop \u2192 `ontouchstart` true, coarse+landscape true, iPhone UA. **PASS**\n2. `#classicGeom*` + `#btnNewXp` create the target geometry \u2192 `126x72 kind=root_blank`. **PASS**\n3. Layer 3 deleted through `.ws-layer-del-btn` \u2192 `layerCount == 3`. **PASS**\n4. Blank baseline matches root_blank exactly \u2192 checkpoint @9, 0 / 27216. **PASS**\n5. Editor-first shell reachable and usable for authoring \u2192 `adv:false`, all 5 controls visible. **PASS**\n6. `#wsApplyGlyph/Fg/Bg` all on before painting \u2192 all three `.ws-toggle-on`. **PASS**\n7. Layer 0 == yellow fill + 7 metadata glyphs \u2192 0 / 9072 @114. **PASS**\n8. Layer 1 == countdown 8..0 period 9 \u2192 0 / 9072 @114. **PASS**\n9\u201319. Eleven L2-progress checkpoints \u2192 each 0 / 27216. **11 \u00d7 PASS**\n20. Layer 2 complete (fill + 2820 art cells, 45 groups, both projection halves) \u2192 0 / 9072 @3029. **PASS**\n21. All 3031 actions execute without error \u2192 3031/3031, 0 errors, 0 unhandled, 0 pageerror. **PASS**\n22. `#wsSaveBtn` \u2192 `save-session` succeeds (export aborts otherwise, `workbench.js:4449-4454`) \u2192 export proceeded. **PASS**\n23. Mobile Share \u2192 `POST /api/workbench/export-xp` 200. **PASS**\n24. Export binary retrievable \u2192 `download-xp` 200, 2452 bytes captured. **PASS**\n25. Exported geometry \u2192 `[126,72,3]`, identical to target. **PASS**\n26. **Decoded payload byte-identical \u2192 both 272192 bytes, sha256 `babb3dc8\u2026c045b`. PASS**\n27. Per-layer per-cell equality \u2192 0 / 27216 mismatches. **PASS**\n\nFAIL: **none.**\n\nINFO:\n\n- I1. gzip wrapper differs (2426 vs 2452 bytes, different sha256). Expected and contractual \u2014 mtime and embedded FNAME are stamped by `gzip.open` and are not reachable from the UI.\n- I2. `navigator.maxTouchPoints` is 0 in this WebKit build even with `hasTouch`; the workbench keys off `ontouchstart` / coarse-pointer instead, both true.\n- I3. The Web Share API branch was taken, so no browser download landed; bytes came from the `download-xp` response the UI itself fetched.\n- I4. One navigation tap outside the recipe: `[data-action=\"toggle-advanced\"]` after step 9. Mutates no document state.\n- I5. Wall time 170.4 s of pure input \u2014 below `audit.md`'s 8\u201325 min projection, because consecutive `tap_cell` actions were dispatched in-page in batches of \u2264120 rather than one CDP round-trip each. The gestures themselves are unchanged (one `pointerdown`/`pointerup` pair per cell).\n- I6. `_getExportedXpBytes()` re-saves the session with `wait_for_idle` before exporting, so the exported bytes are the persisted server-side document, not a client-side serialisation.\n\n### 13.9 Honest caveats\n\na. **Synthetic PointerEvents for canvas work.** The 2827 cell taps and 74 drags are\n   `new PointerEvent(..., pointerType:'touch')` dispatched on `#wholeSheetCanvas`, not\n   driver-level hardware touch. Two reasons, both structural: the WebKit automation protocol\n   exposes only `Input.dispatchTapEvent` (already recorded in \u00a711), and at 126 \u00d7 72 cells \u00d7 12 px\n   the sheet is 1512 \u00d7 864 CSS px inside a 750 \u00d7 342 viewport, so hardware-coordinate taps would\n   require a pan or zoom operation per cell. All twelve *control* taps in the recipe were real\n   `page.tap()`.\nb. **No physical hardware.** No real device, no real finger, no OS share sheet was dismissed by a\n   human. The share call was accepted by the WebKit context; nothing verifies what an iOS share\n   sheet would then do with the file.\nc. **`wm-04-after-L0.png` comes from a second, separate 29-step run**\n   (`execute_l0_shot.mjs`), not from the main run. In the main run the step-29 screenshot hook sat\n   behind the batched `tap_cell` branch and never fired \u2014 a driver bug, not a UI finding. The\n   supplementary run re-executed recipe steps 1\u201329 in the same mobile context and verified\n   `{\"layers\":3,\"cols\":126,\"rows\":72,\"L0_mismatches\":0}` before capturing. Layer 0 correctness *in\n   the main run* is established by the step-114 checkpoint, which re-verifies all three layers.\nd. **Colour inputs were value-seeded.** `#wsFgColor` / `#wsBgColor` are `input[type=color]`; their\n   pickers are OS-native and undrivable. The 51 colour changes set `.value` and dispatched\n   `input` + `change`, which is what the handlers listen on. Every other field was really typed.\ne. **`select_layer` used `element.click()`** on the real `.ws-layer-row`, not `page.tap()` \u2014 the row\n   is a real control with its own click handler, but the tap was not driver-level.\nf. **The recipe, not free-hand authoring.** This run proves the recipe reproduces the target through\n   the mobile UI. It does not claim a human would discover this 3031-action sequence unaided; the\n   obstacle table in `audit.md` \u00a75 documents what made it this long (chiefly the defective `Flip V`,\n   which forces both projection halves to be painted cell by cell).\ng. **Layer 2 art was painted with the transparent fill already down**, so the 2820 art taps overwrite\n   fill cells rather than empty ones \u2014 matching the simulation, and confirmed by every checkpoint.\n\n### 13.10 Teardown\n\nServer killed after the run; port 5071 left with no listener (verified below).\nMDEOF\necho appended; wc -l .scratch/2026-08-13-headed-proof.md",
  "preview": "cat >> .scratch/2026-08-13-headed-proof.md <<'MDEOF' --- ## 13. Wallace from-scratch mobile recipe \u2014 live execution Phase-2 execution of `.scratch/2026-08-13-wallace-recipe/recipe.json` (3031 actions) end-to-end through the real headed **mobile** WebKit workbench, followed by an export through th..."
}
```
